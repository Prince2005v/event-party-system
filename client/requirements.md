## Packages
framer-motion | Smooth page transitions and component animations
recharts | Dashboard analytics charts for spending and booking stats
date-fns | Formatting dates for event schedules and booking history
uuid | Generating session keys for guest users

## Notes
Authentication uses Replit Auth (useAuth hook)
Persistent selection uses localStorage to store session_key for guests
Images use Unsplash placeholders with descriptive keywords
Dark mode is the default and only theme
