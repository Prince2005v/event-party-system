# Server Environment Variables
PORT=5000
NODE_ENV=development

# Database Connection (PostgreSQL / Neon DB)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/eventify_db

# Session Security
SESSION_SECRET=eventify_planner_super_secret_key_2026

# Replit Auth (Optional when deploying on Replit)
REPL_ID=your_repl_id
ISSUER_URL=https://replit.com/oidc
